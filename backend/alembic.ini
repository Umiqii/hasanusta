[alembic]
# A generic script location relative to the ini file
script_location = alembic
# script_location = myapp:migrations

# ... other alembic settings ...

# String template for migration file names. See the help for
# -o/--output-options for details on available substitutions.
# file_template = %%(rev)s_%%(slug)s

# Logging configuration
[loggers]
# Handlers
[handlers]
keys = console

[formatters]
# Set to 'true' to run the environment during
# the 'check' workflow, command-
# default is 'false'
# run_environment = false

# sqlalchemy.url = postgresql://user:password@host/dbname
# sqlalchemy.url satırını yorum satırı yapıyoruz:
# sqlalchemy.url = postgresql://user:password@host/dbname

# Other configuration options, e.g. associated with third party
# extensions:
# myext.option = value

# Environment Variables
# Define environment variables accessible to env.py, useful for database connection
# For example, pulling the DATABASE_URL from the environment
[post_write_hooks]
# This section might be useful later for formatting etc.

# Set the sqlalchemy.url using an environment variable
# Make sure your .env file is loaded or DATABASE_URL is set in the environment
# when running alembic commands.
# sqlalchemy.url = ${DATABASE_URL} # Bu satırı yorumluyoruz, env.py'de ayarlanacak

# You might need to add other options required by your database driver 