# Frontend Environment Variables Example

# API Base URL (if needed, otherwise use relative paths like /api)
# Vite requires variables exposed to the browser to start with VITE_
# Example: If calling API from frontend like `${import.meta.env.VITE_API_BASE_URL}/users`
# With the current Nginx proxy setup, relative paths `/api/...` should work directly.
# VITE_API_BASE_URL="/api"

# Other frontend-specific variables (e.g., analytics keys)
# VITE_GOOGLE_ANALYTICS_ID=G-XXXXXXXXXX